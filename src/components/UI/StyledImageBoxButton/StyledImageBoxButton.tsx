import { IconButton } from "@mui/material";
import { styled } from "@mui/material/styles";

const StyledImageBoxButton = styled(IconButton)`
  color: rgba(255, 255, 255, 0.54);
`;

export default StyledImageBoxButton;
